# Perform line feed normalization on text files.
*                   text=auto

# Specify which line feed character sequence to use for specific files.
*.conf      text    eol=lf
*.network   text    eol=lf
*.sh        text    eol=lf
